export interface NavbarProps {
  /** @startingPoint section="Components" subtitle="Marketing site top nav — default & on-ground" viewport="900x80" */
  links?: string[];
  cta?: React.ReactNode;
  onGround?: boolean;
}
