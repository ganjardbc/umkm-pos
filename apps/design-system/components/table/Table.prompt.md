Data table for transactions, stock, and orders — pair numeric columns with `mono: true` for tabular alignment.
```jsx
<Table columns={[{key:'id',label:'ID'},{key:'total',label:'Total',align:'right',mono:true}]} rows={data} />
```
