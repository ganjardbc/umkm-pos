export interface ProgressProps {
  /** @startingPoint section="Components" subtitle="Determinate bar — stock levels, upload progress" viewport="400x40" */
  value?: number;
  tone?: 'brand' | 'warning' | 'error';
}
export interface SpinnerProps {
  /** @startingPoint section="Components" subtitle="Indeterminate loading spinner" viewport="80x80" */
  size?: number;
}
