Popover menu anchored to a trigger element.
```jsx
<Dropdown trigger={<Button variant="ghost">Aksi ⌄</Button>} items={[{id:'edit',label:'Edit'},{id:'hapus',label:'Hapus',danger:true}]} onSelect={handle} />
```
