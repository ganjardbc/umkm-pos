# Tasks: GAN-120 - Convert Outlets list to card view

## Frontend Tasks
- [x] (apps/web) Replace `<DataTable>` in `apps/web/src/modules/outlet/pages/index.vue` with responsive card grid using `UiCard`, `UiLoading`, and empty state container
- [x] (apps/web) Implement outlet card layout displaying Logo, Name, Merchant, Location, Created At, and Status badge
- [x] (apps/web) Implement card action buttons (Detail, Edit, Delete) preserving RBAC permissions (`isCanUpdate`, `isCanDelete`) and click handlers
- [x] (apps/web) Verify responsive behavior across mobile/desktop, search, pagination, and deletion flow
