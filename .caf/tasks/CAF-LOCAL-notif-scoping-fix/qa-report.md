## QA Report — CAF-LOCAL-notif-scoping-fix
Ticket: CAF-LOCAL-notif-scoping-fix
Agent: caf-qa
Status: PASS

### Verification Matrix
| # | Acceptance Criteria (requirements.md) | How Verified | Result |
|---|---------------------------------------|--------------|--------|
| 1 | `GET /api/v1/notification` with `X-Outlet-Id: A` returns only notifications with `outlet_id` A and `meta.unreadCount` counts only those; no header returns both outlets' notifications. | Manual code walk: `apps/api/src/notifications/notifications.service.ts:14-50` `findAll` — all three queries (`findMany`, `count`, unread `count`) spread `...(outletId ? { outlet_id: outletId } : {})`, so an absent `outletId` (guard sets nothing when header absent) omits the filter on all three, and a present `outletId` (set by `OutletHeaderGuard` only after it validates the outlet belongs to the caller and merchant) filters all three identically, including the previously-easy-to-miss second `unreadCount` query. No live DB to run the HTTP request end-to-end — verified by static reading, not integration test. | PASS |
| 2 | `X-Outlet-Id` naming another merchant's outlet, an outlet the user has no `user_roles` row for, or a non-existent id is rejected with 403, not silently ignored, not 500. | Read `apps/api/src/common/guards/outlet-header.guard.ts:34-68`: all three failure branches (outlet missing, `merchant_id` mismatch, no `user_roles` row) throw the same `ForbiddenException('Invalid outlet context')` (→ HTTP 403), and the surrounding `try/catch` converts any other thrown error (malformed id, DB error) to the same exception, never letting it propagate as a 500. Confirmed by `apps/api/src/common/guards/outlet-header.guard.spec.ts` (6 cases: absent, valid, wrong-merchant, no-role, non-existent, malformed/DB-error) — ran via `pnpm --filter umkm-pos-api run test`, all pass. | PASS |
| 3 | `GET /api/v1/notification/:id` and `PATCH /api/v1/notification/:id/read` for a notification belonging to another merchant return 403 even with a correctly-guessed id. | `notifications.service.ts:52-72` `findOne` checks `notification.merchant_id !== merchantId` (merchantId taken from `@CurrentUser('merchant_id')`, JWT-derived, never client input) and throws the existing `ForbiddenException`; `markAsRead` (line 74-85) calls `findOne` first, so it inherits the same check before ever reaching `update`. Confirmed by `apps/api/src/notifications/notifications.service.spec.ts` cross-merchant test, run via `pnpm --filter umkm-pos-api run test` (pass). | PASS |
| 4 | `notifications.merchant_id` exists, NOT NULL, backfilled from `user_id`'s merchant; migration runs cleanly on a table with existing rows. | Read `apps/api/prisma/schema.prisma` diff (merchant_id Char(36) NOT NULL, FK `notifications_ibfk_3`, composite index `idx_notifications_merchant_user_read`) and `apps/api/prisma/migrations/20260912000000_add_merchant_id_to_notifications/migration.sql`: correct ordering (1: add nullable column, 2: backfill via `INNER JOIN users` on `user_id`, 3: `MODIFY COLUMN ... NOT NULL` which fails atomically if any row is still NULL rather than the alternative of silently succeeding, 4: FK, 5: index). Schema and migration SQL agree (column type/nullability, FK name, index columns/order all match). No live database was available to actually execute the migration — this is expected per the ticket's own instructions and is not a finding; assessed by static SQL review only, so "runs cleanly on a table with existing rows" is verified in principle (INNER JOIN correctly matches every FK-protected row) but not empirically executed. | PASS (verified statically; execution against a live DB unverified — see Notes) |
| 5 | Customer order via customer catalog creates notification rows only for `transaction.read`-holding users at that outlet, each with correct `merchant_id`; an `outletId` from another merchant throws and writes nothing. | `apps/api/src/customer-catalog/customer-catalog.service.ts:236-244` calls `notifyOutletUsers(dto.outlet_id, session.merchant_id, {...}, 'transaction.read')` where `session.merchant_id` comes from `resolveSession()` (`customer-catalog.service.ts:310-335`), a DB-loaded `customer_sessions` record — genuinely server-derived, never client input. `notifications.service.ts:120-157` `notifyOutletUsers`: calls `assertOutletBelongsToMerchant` first (throws `UnauthorizedException` before any query if outlet belongs to another merchant, so nothing is written), then filters `user_roles` by `roles.role_permissions.some({ permissions: { code: requiredPermission } })`, then stamps `merchant_id` on every created row. Confirmed by `notifications.service.spec.ts` (`notifyOutletUsers` wrong-merchant / permission-filtered / no-permission-code tests), run via `pnpm --filter umkm-pos-api run test` (pass). | PASS |
| 6 | `apps/web` sends `X-Outlet-Id` when `APP_ACTIVE_OUTLET` is set, omits it cleanly when not; no CORS preflight failure from the new header. | `apps/web/src/plugins/axios.ts:24-28`: `const outlet = getOutlet(); if (outlet?.id) config.headers['X-Outlet-Id'] = outlet.id;`. `getOutlet()` (`apps/web/src/helpers/auth.ts:70-73`) returns `{}` when `APP_ACTIVE_OUTLET` is unset, so `outlet?.id` is falsy and the header is never assigned (not sent empty/null/"undefined"). `apps/api/src/main.ts:24-28`: `app.enableCors({ ..., allowedHeaders: ['Content-Type', 'Authorization', 'X-Outlet-Id'] })` — explicit allow-list now includes the new header, so preflight will not block it. No running dev server to observe an actual preflight round-trip — verified by static code review, not a live browser request. | PASS |
| 7 | Existing behaviour preserved: `pnpm --filter umkm-pos-api run test` passes including `transactions.service.spec.ts`; lint and build pass for both apps. | Ran directly: `pnpm --filter umkm-pos-api run test` → 21 suites / 234 tests passed (transactions.service.spec.ts included, confirmed unmodified via `git status --porcelain` showing no change to that file). `pnpm --filter umkm-pos-api run lint` → clean, no output/errors. `pnpm --filter umkm-pos-api run build` → clean (`nest build`). `pnpm --filter umkm-pos-app run build` → clean (`vue-tsc -b && vite build`), only a pre-existing chunk-size warning unrelated to this change. `pnpm --filter umkm-pos-app run lint` → "None of the selected packages has a lint script" (confirmed via `package.json`: only `dev`/`build`/`preview`/`new-module` scripts exist — there genuinely is nothing to run here; `build`'s `vue-tsc -b` step is the closest available type-error signal and it passed). | PASS |
| 8 | New unit tests cover outlet-context validation (valid / wrong-merchant / no-role / absent) and merchant scoping of `findOne`. | `apps/api/src/common/guards/outlet-header.guard.spec.ts` covers absent, valid, wrong-merchant, no-role, non-existent, and malformed/DB-error cases (6 tests, all passing). `apps/api/src/notifications/notifications.service.spec.ts` covers `findOne` same-merchant success and cross-merchant `ForbiddenException`, plus `notifyOutletUsers` wrong-merchant/permission-filter/no-permission-code cases. All ran and passed via `pnpm --filter umkm-pos-api run test`. | PASS |

### Findings
None.

### Notes
- AC4's migration was assessed by static SQL/schema review only; no MySQL instance was available in this environment to actually execute `prisma migrate deploy`/`dev` against a database with pre-existing rows, so "runs cleanly" is a code-review conclusion, not an empirically observed one. This matches what both implementation agents' `verify-report.md` state and is expected per the ticket's own environment constraints — not treated as a failure, but flagging it as unverified-by-execution per the QA agent's obligation not to claim untested behaviour passed.
- AC1's end-to-end HTTP behaviour (two real outlets, a real JWT, and an actual `GET` request) and AC6's actual CORS preflight round-trip were likewise verified by reading the guard/service/interceptor code and the accompanying unit tests, not by hitting a running server — no live API/DB was available in this environment. The code reads correctly and the relevant unit tests exercise the same logic paths, so I'm recording these as PASS, but a full manual/integration pass against a running stack (seeded two-outlet merchant, real JWT, curl with/without `X-Outlet-Id`) is recommended before this ships to catch anything a unit-level mock might not (e.g. Prisma query shape mistakes that only surface against a real schema).
- Confirmed no collateral damage: `OutletHeaderGuard` is registered only via `@UseGuards(PermissionGuard, OutletHeaderGuard)` on `NotificationsController` (`apps/api/src/notifications/notifications.controller.ts:25`) — `app.module.ts`'s only `APP_GUARD` provider is `JwtAuthGuard` (pre-existing), and no other controller references `OutletHeaderGuard`. `apps/web/src/components/UiSidebarNotification.vue` and `apps/web/src/modules/notification/**` show no changes in `git status --porcelain`. `transactions.service.spec.ts` is untouched and passes.
- `OutletHeaderGuard` is not explicitly listed in `NotificationsModule`'s `providers` array, matching the existing (pre-ticket) pattern for `PermissionGuard`, which is likewise never registered as an explicit provider in any consuming module — this works because `PrismaService` comes from the `@Global()` `DatabaseModule`, so Nest's DI can resolve the guard's constructor dependency without an explicit provider entry. Build and all 234 tests pass, confirming this resolves correctly at compile/test time; this is consistent with pre-existing conventions in the codebase, not a new risk introduced by this ticket.
